# Vite environment variables for Ontology Engine frontend
# Copy to .env.local and fill in values

VITE_SUPABASE_URL=https://your-project.supabase.co   # Dashboard → Settings → API → Project URL
VITE_SUPABASE_ANON_KEY=CHANGEME                       # Dashboard → Settings → API → anon/public key
VITE_API_BASE_URL=http://localhost:8000
